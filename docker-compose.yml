version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: recoveros-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-recoveros}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-recoveros_dev_2024}
      POSTGRES_DB: ${POSTGRES_DB:-recoveros}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U recoveros"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  pgdata:
    driver: local
