{
  "name": "veyro",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix apps/web run build",
    "dev": "npm --prefix apps/web run dev",
    "start": "npm --prefix apps/web start"
  },
  "dependencies": {
    "next": "^14.2.5"
  }
}
